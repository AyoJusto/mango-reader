-- v6 -> v7: chapter/library update tracking (see catalog_cache.sq and library.sq for column notes).

ALTER TABLE chapter_cache ADD COLUMN first_seen_at INTEGER NOT NULL DEFAULT 0;
ALTER TABLE library_item ADD COLUMN last_opened_at INTEGER NOT NULL DEFAULT 0;
